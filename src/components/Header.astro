---
import { NAV_LINKS, SITE_CONFIG } from '../data/site-config.ts';
import LanguageSwitcher from './LanguageSwitcher.astro';

const currentPath = Astro.url.pathname;
const isDefaultLocale = !currentPath.match(/^\/(pt|nl|fr)\//);
const localeMatch = currentPath.match(/^\/(pt|nl|fr)\//);
const locale = localeMatch ? localeMatch[1] : 'en';
const navLinks = NAV_LINKS[locale as keyof typeof NAV_LINKS] || NAV_LINKS.en;
const rootPath = isDefaultLocale ? '/' : `/${locale}/`;

const isActive = (path: string) => {
  if (path === rootPath) return currentPath === rootPath || currentPath === rootPath.replace(/\/$/, '');
  return currentPath.startsWith(path);
};
---

<header class="sticky top-0 z-50 bg-white/95 backdrop-blur-sm border-b border-warm-sand">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 py-4 flex items-center justify-between">
    <a href={rootPath} class="text-lg font-bold tracking-tight text-terracotta-500 hover:text-terracotta-600 transition-colors">
      {SITE_CONFIG.name}
    </a>
    
    <!-- Desktop nav -->
    <div class="hidden md:flex items-center gap-1">
      {navLinks.map(link => (
        <a
          href={link.path}
          class={`px-3 py-2 text-sm rounded-full transition-colors ${
            isActive(link.path)
              ? 'text-olive-900 font-semibold bg-olive-50'
              : 'text-olive-700 hover:text-olive-900 hover:bg-olive-50'
          }`}
        >
          {link.label}
        </a>
      ))}
    </div>

    <!-- Shared language selector + mobile nav toggle -->
    <div class="flex items-center gap-2 ml-3 pl-3 border-l border-warm-sand">
      <LanguageSwitcher />
      <button id="menu-toggle" class="md:hidden p-2 text-slate-warm rounded-full hover:bg-olive-50" aria-label="Menu">
        <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="3" y1="6" x2="21" y2="6"></line>
          <line x1="3" y1="12" x2="21" y2="12"></line>
          <line x1="3" y1="18" x2="21" y2="18"></line>
        </svg>
      </button>
    </div>
  </nav>
  
  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-warm-sand">
    <div class="px-4 py-2 space-y-1">
      {navLinks.map(link => (
        <a
          href={link.path}
          class={`block px-3 py-2 text-sm rounded-full ${
            isActive(link.path)
              ? 'text-olive-900 font-semibold bg-olive-50'
              : 'text-olive-700 hover:text-olive-900'
          }`}
        >
          {link.label}
        </a>
      ))}
    </div>
  </div>
</header>

<script>
  const toggle = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');
  if (toggle && menu) {
    toggle.addEventListener('click', () => {
      menu.classList.toggle('hidden');
    });
  }
</script>
